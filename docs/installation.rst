============
Installation
============

At the command line::

    $ easy_install banking.statements

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv banking.statements
    $ pip install banking.statements
